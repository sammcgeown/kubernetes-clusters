apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - ../../base
  - argocd-server-tls.yaml
  - argocd-ingress.yaml
  # Keep only the sealed cluster secret in Git-tracked manifests.
  - rpi4-cluster-sealedsecret.yaml
