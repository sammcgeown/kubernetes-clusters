apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  - ../../base
  - argocd-server-tls.yaml
  - argocd-ingress.yaml
  # rpi4 cluster is offline - cluster secret disabled so Argo CD stops
  # retrying connections to it. Kept in Git for later redeployment; re-add
  # this resource (and the rpi4 generator entry in appsets/infrastructure.yaml)
  # when the cluster is back.
  # - rpi4-cluster-sealedsecret.yaml
